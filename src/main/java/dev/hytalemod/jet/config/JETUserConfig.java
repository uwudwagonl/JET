package dev.hytalemod.jet.config;

/**
 * Per-player configuration for JET plugin.
 * Each player gets their own instance with personal preferences.
 */
public class JETUserConfig {
    public boolean bindAltKey = true;
    public String backgroundImage = "none";
    public float backgroundOpacity = 0.8f;

    public JETUserConfig() {
    }
}
